DROP TABLE IF EXISTS chores;
CREATE TABLE chores (
    id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    householdId INTEGER REFERENCES household(id) ON DELETE CASCADE NOT NULL,
    points INTEGER NOT NULL,
    chore CHARACTER VARYING NOT NULL,
    done BOOLEAN,
    member_name CHARACTER VARYING REFERENCES members(name) ON DELETE CASCADE NOT NULL,
);